```text
sudo vi /etc/shanty-monitor/agent.yaml
sudo systemctl restart shanty-agent

sudo vi /etc/shanty-monitor/config.yaml
sudo systemctl restart shanty-collector

for remote servers the "collector_url" must be the domain name

More to come...
```
